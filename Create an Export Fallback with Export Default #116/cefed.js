/* Create an Export Fallback with Export Default */

// There is also something called export default. This is a fallback export. And it's often used if you only want to export one thing from a file.

export default function subract(x,y) {return x - y;}